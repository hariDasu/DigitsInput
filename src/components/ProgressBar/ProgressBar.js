import React from 'react';
import * as Progress from 'react-native-progress';


const ProgressBar = () => (
    <Progress.Bar progress={0.25} width={250} unfilledColor={'lightgrey'} borderWidth={0} borderRadius={0} color={'#308C6A'} height={4}/>
  );




export default ProgressBar;
